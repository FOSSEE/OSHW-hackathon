<nav id="site-nav" class="fixed top-0 inset-x-0 z-50 bg-transparent text-white transition-all duration-300">
  <div class="max-w-7xl mx-auto px-6">
    <div class="h-16 flex items-center justify-between">
      <div class="flex items-center gap-3">
        <div class="flex items-center gap-2">
          <div class="h-8 w-8 rounded bg-sky-500/90 text-white grid place-items-center text-xs font-bold">
     
          </div>
       
        </div>
        <span class="opacity-60">|</span>
        <div class="flex items-center gap-2">
          <div class="h-8 w-8 rounded-full border border-current text-current grid place-items-center text-[10px] font-semibold opacity-80">
            IIT
          </div>
         
        </div>
      </div>

      <div class="hidden md:flex items-center gap-8 text-sm">
        <a href="#home" class="text-current opacity-90 hover:opacity-100 transition">Home</a>
        <a href="#themes" class="text-current opacity-90 hover:opacity-100 transition">Themes</a>
        <a href="#why" class="text-current opacity-90 hover:opacity-100 transition">About</a>
      </div>

      <a
        href="#register"
        class="rounded-md bg-orange-500 px-4 py-2 text-white font-semibold shadow-sm hover:bg-orange-600 transition"
      >
        Register Now
      </a>
    </div>
  </div>
</nav>

<script>
  const nav = document.getElementById("site-nav");
  const setNav = () => {
    if (!nav) return;
    const scrolled = window.scrollY > 40;
    if (scrolled) {
      nav.classList.add(
        "bg-white/95",
        "backdrop-blur",
        "shadow-sm",
        "border-b",
        "border-slate-200",
        "text-slate-900"
      );
      nav.classList.remove("bg-transparent", "text-white");
    } else {
      nav.classList.add("bg-transparent", "text-white");
      nav.classList.remove(
        "bg-white/95",
        "backdrop-blur",
        "shadow-sm",
        "border-b",
        "border-slate-200",
        "text-slate-900"
      );
    }
  };
  window.addEventListener("scroll", setNav, { passive: true });
  setNav();
</script>
